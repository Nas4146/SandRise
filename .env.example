# Astro Public Environment Variables
# Copy to .env and fill in after deploying the Cloudflare Worker

# Worker feed endpoint (set after deploying worker)
# PUBLIC_EXPLORING_FEED_URL=https://exploring-ingest.your-subdomain.workers.dev/feed

# Uncomment and set once worker is deployed:
# PUBLIC_EXPLORING_FEED_URL=
