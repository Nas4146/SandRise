---
import BaseLayout from '@layouts/BaseLayout.astro';
import SectionHeading from '@components/SectionHeading.astro';
import TrustedByLogos from '@components/TrustedByLogos.astro';
import profilePortrait from '@content/profile.jpeg';
import type { CollectionEntry } from 'astro:content';
import { getCollection } from 'astro:content';

const allProjects = (await getCollection('projects')).filter((project) => !project.data.draft);
const projects: CollectionEntry<'projects'>[] = allProjects.sort((a, b) =>
  a.data.title.localeCompare(b.data.title)
);

const interests = [
  'AI & applied automation',
  'Product systems & ops',
  'Strength training',
  'Coffee brewing & roasting',
  'Horror films & storytelling',
  'Trail running & outdoors'
];

const heroStructuredData = {
  '@context': 'https://schema.org',
  '@type': 'Person',
  name: 'Nick Sanders',
  jobTitle: 'Product Builder',
  url: 'https://sandrise.studio',
  worksFor: {
    '@type': 'Organization',
    name: 'SandRise'
  }
};
---
<BaseLayout
  title="Nick Sanders — Product Builder @ SandRise"
  description="Product builder and founder of SandRise, crafting digital products that validate quickly and scale responsibly."
  structuredData={heroStructuredData}
>
  <section id="intro" class="intro-section pattern-grid">
    <div class="container intro-grid">
      <div class="intro-copy">
        <h1 class="intro-name">Nick Sanders</h1>
        <p class="intro-title">Product Builder</p>
        <p class="intro-location">San Francisco Bay Area</p>
        <p class="intro-skills">Product Management | Cloud Platform & Services | Developer Tools | AI Agents | Ops Design</p>
        <a class="intro-email" href="mailto:Nick.sanders.a@gmail.com">Nick.sanders.a@gmail.com</a>
        <p class="intro-bio">
          DC roots, Austin for business school, now building from the SF Bay Area. Number of years in product management and developer tools. Currently obsessed with AI and agent-powered experiences, and shaping a slate of AI enabled products.
        </p>
        <TrustedByLogos />
        <div class="intro-actions">
          <a class="intro-button primary" href="/resume.pdf" download target="_blank" rel="noopener">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
              <polyline points="14,2 14,8 20,8"/>
              <line x1="16" y1="13" x2="8" y2="13"/>
              <line x1="16" y1="17" x2="8" y2="17"/>
              <polyline points="10,9 9,9 8,9"/>
            </svg>
            Download resume
          </a>
          <a class="intro-button" href="https://github.com/Nas4146" target="_blank" rel="noopener">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
            </svg>
            GitHub
          </a>
          <a class="intro-button" href="https://www.linkedin.com/in/sandersnick/" target="_blank" rel="noopener">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
              <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
            </svg>
            LinkedIn
          </a>
        </div>
      </div>
      <aside class="intro-portrait">
        <div class="portrait-frame">
          <img
            src={profilePortrait.src}
            alt="Portrait of Nick Sanders"
            width={profilePortrait.width}
            height={profilePortrait.height}
            loading="lazy"
            decoding="async"
          />
        </div>
      </aside>
    </div>
  </section>

  <section id="projects" class="projects-section scroll-trigger">
    <div class="container">
      <h2 class="projects-heading scroll-fade-up">Current Projects</h2>
      <ul class="project-list">
        {projects.map((project, index) => {
          const href = project.data.href ?? `/work/${project.slug}`;
          return (
            <li>
              <a
                class="project-tile scroll-scale-in"
                href={href}
                aria-label={`Explore ${project.data.title}`}
                style={`--project-image: url(${project.data.heroImage}); --animation-delay: ${index * 0.1}s`}
              >
                <div class="project-overlay">
                  <span class="project-index">{String(index + 1).padStart(2, '0')}</span>
                  <div class="project-content">
                    <div class="project-heading">
                      <h3>{project.data.title}</h3>
                      {project.data.impact && <span class="project-impact">{project.data.impact}</span>}
                    </div>
                    <p>{project.data.summary}</p>
                    {project.data.tags && project.data.tags.length > 0 && (
                      <div class="project-tags">
                        {project.data.tags.slice(0, 4).map((tag) => (
                          <span>{tag}</span>
                        ))}
                      </div>
                    )}
                  </div>
                </div>
              </a>
            </li>
          );
        })}
      </ul>
    </div>
  </section>

  <section id="interests" class="section-compact interests-section scroll-trigger">
    <div class="container">
      <div class="scroll-fade-up">
        <SectionHeading
          eyebrow="Interests"
          title="What keeps me curious right now"
          description="The themes I'm reading, building, and experimenting with outside of client work."
        />
      </div>
      <ul class="interests-grid">
        {interests.map((interest, index) => (
          <li class="interest-card scroll-slide-in" style={`--animation-delay: ${index * 0.1}s`}>
            <span>{interest}</span>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <section id="sandrise" class="section-alt sandrise-section pattern-grid scroll-trigger">
    <div class="container sandrise-grid">
      <div class="sandrise-copy scroll-fade-left">
        <SectionHeading
          eyebrow="SandRise"
          title="The studio I founded to shape momentum"
          description="SandRise is a lean product practice focused on finding signal fast, designing what matters, and guiding teams through the messy middle of shipping."
        />
        <p>
          I partner with founders and product leaders to translate ambition into systems, rituals, and experiences that customers trust. Whether it's validating a new market, building the first design system, or accelerating growth, SandRise brings an operator's mindset with an IC's hands-on craft.
        </p>
      </div>
      <div class="sandrise-card card scroll-fade-right">
        <h3>How we engage</h3>
        <ul>
          <li><strong>Fractional product leadership</strong> for teams that need strategy and execution in the same seat.</li>
          <li><strong>Discovery sprints</strong> that surface customer signal and prioritize the next build.</li>
          <li><strong>Design systems & delivery</strong> to scale craft without slowing velocity.</li>
          <li><strong>Founder advisory</strong> supporting GTM, hiring, and operating cadence.</li>
        </ul>
      </div>
    </div>
  </section>

</BaseLayout>

<script>
  // Scroll-triggered animations using Intersection Observer
  document.addEventListener('DOMContentLoaded', () => {
    // Check if user prefers reduced motion
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    
    if (prefersReducedMotion) {
      return; // Exit early if reduced motion is preferred
    }

    // Create intersection observer for scroll animations
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          // Add animate class to trigger the animation
          entry.target.classList.add('animate');
          
          // Handle staggered animations for children
          const children = entry.target.querySelectorAll('.scroll-scale-in, .scroll-slide-in');
          children.forEach((child, index) => {
            setTimeout(() => {
              child.classList.add('animate');
            }, index * 100); // 100ms stagger delay
          });
          
          // Stop observing once animated
          observer.unobserve(entry.target);
        }
      });
    }, observerOptions);

    // Observe all elements with scroll animation classes
    const scrollElements = document.querySelectorAll(
      '.scroll-fade-up, .scroll-fade-left, .scroll-fade-right, .scroll-scale-in, .scroll-slide-in'
    );

    scrollElements.forEach((el) => {
      observer.observe(el);
    });

    // Special handling for project tiles and interest cards
    const projectTiles = document.querySelectorAll('.project-tile');
    const interestCards = document.querySelectorAll('.interest-card');

    // Observe project tiles individually for staggered animation
    projectTiles.forEach((tile) => {
      observer.observe(tile);
    });

    // Observe interest cards individually for staggered animation  
    interestCards.forEach((card) => {
      observer.observe(card);
    });
  });
</script>

<style>
  .intro-section {
    padding-top: var(--space-md);
    padding-bottom: var(--space-md);
    min-height: 50vh;
    display: flex;
    align-items: center;
  }

  .intro-grid {
    display: grid;
    grid-template-columns: repeat(12, minmax(0, 1fr));
    gap: var(--space-md);
    align-items: center;
    width: 100%;
    min-height: 100%;
  }

  .intro-copy {
    grid-column: span 8;
    display: grid;
    gap: 0;
    background-color: rgba(255, 255, 255, 0.18);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    padding: var(--space-lg);
    border-radius: var(--radius-lg);
    border: 1px solid rgba(255, 255, 255, 0.2);
    position: relative;
    box-shadow: 
      0 8px 32px rgba(0, 0, 0, 0.1),
      inset 0 1px 0 rgba(255, 255, 255, 0.2);
    align-items: start;
    justify-items: start;
    align-self: stretch;
    animation: slideInLeft 0.8s ease-out;
    overflow: hidden;
  }

  .intro-copy::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background-color: rgba(255, 255, 255, 0.6);
    border-radius: var(--radius-lg) var(--radius-lg) 0 0;
  }

  .intro-copy::after {
    display: none;
  }

  .intro-name {
    margin: 0;
    font-size: clamp(1.5rem, 2.5vw, 2.25rem);
    letter-spacing: -0.02em;
    color: var(--color-text-primary);
    font-weight: var(--font-weight-bold);
    position: relative;
    text-shadow: 0 1px 2px rgba(12, 143, 98, 0.1);
  }

  .intro-name::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 60px;
    height: 3px;
    background-color: var(--color-accent);
    border-radius: var(--radius-round);
    box-shadow: 0 2px 4px rgba(79, 124, 99, 0.18);
  }

  .intro-title {
    margin: 0;
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-semibold);
    color: var(--color-accent);
    margin-bottom: var(--space-xs);
    position: relative;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-top: var(--space-sm);
  }

  .intro-title::before {
    content: '◆';
    position: absolute;
    left: -20px;
    top: 50%;
    transform: translateY(-50%);
    color: var(--color-accent);
    font-size: 0.6em;
    opacity: 0.7;
  }

  .intro-location {
    margin: 0;
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-medium);
    color: var(--color-accent);
    margin-bottom: var(--space-2xs);
    opacity: 0.8;
  }

  .intro-skills {
    margin: 0;
    font-size: var(--font-size-xs);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--color-text-secondary);
    font-weight: var(--font-weight-medium);
    margin-bottom: var(--space-xs);
    padding-left: var(--space-sm);
    border-left: 3px solid var(--color-accent);
    background-color: rgba(79, 124, 99, 0.06);
    padding-top: var(--space-2xs);
    padding-bottom: var(--space-2xs);
    border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
  }

  .intro-email {
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-medium);
    color: var(--color-text-secondary);
    text-decoration: none;
    position: relative;
    transition: all 0.3s ease;
    margin-bottom: var(--space-2xs);
    padding: var(--space-2xs) var(--space-xs);
    background: rgba(93, 138, 111, 0.02);
    border-radius: var(--radius-sm);
    width: fit-content;
    transform: translateY(0);
  }

  .intro-email:hover,
  .intro-email:focus-visible {
    color: var(--color-accent);
    background: rgba(93, 138, 111, 0.05);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(12, 143, 98, 0.15);
  }

  .intro-bio {
    margin: 0;
    font-size: var(--font-size-sm);
    line-height: 1.6;
    color: var(--color-text-primary);
    padding-left: var(--space-xs);
    border-left: 2px solid rgba(79, 124, 99, 0.18);
  }

  .intro-copy :global(.trusted-by-logos) {
    margin: 0;
  }

  .intro-actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2xs);
    margin-top: var(--space-md);
    justify-content: flex-start;
  }

  .intro-button {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2xs);
    padding: var(--space-xs) var(--space-md);
    border-radius: var(--radius-round);
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-semibold);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    text-decoration: none;
    color: var(--color-text-primary);
    background-color: rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    transition: transform var(--duration-sm) var(--ease-standard),
      box-shadow var(--duration-sm) var(--ease-standard),
      border-color var(--duration-sm) var(--ease-standard);
  }

  .intro-button.primary {
    background-color: rgba(79, 124, 99, 0.26);
    border-color: rgba(79, 124, 99, 0.4);
    color: var(--color-text-primary);
  }

  .intro-button:hover,
  .intro-button:focus-visible {
    transform: translateY(-2px);
    border-color: rgba(255, 255, 255, 0.4);
    box-shadow: 
      0 8px 25px rgba(0, 0, 0, 0.15),
      inset 0 1px 0 rgba(255, 255, 255, 0.3);
  }

  .intro-button svg {
    flex-shrink: 0;
  }

  .intro-portrait {
    grid-column: span 4;
    position: relative;
    min-height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    animation: slideInRight 0.8s ease-out;
  }

  .portrait-frame {
    position: relative;
    padding: var(--space-xs);
    border-radius: var(--radius-lg);
    background-color: rgba(255, 255, 255, 0.18);
    backdrop-filter: blur(15px);
    -webkit-backdrop-filter: blur(15px);
    border: 1px solid rgba(255, 255, 255, 0.3);
    box-shadow: 
      0 8px 32px rgba(0, 0, 0, 0.1),
      inset 0 1px 0 rgba(255, 255, 255, 0.3);
    transition: all 0.4s ease;
  }

  .portrait-frame:hover {
    transform: translateY(-4px);
    box-shadow: 
      0 12px 48px rgba(0, 0, 0, 0.15),
      inset 0 1px 0 rgba(255, 255, 255, 0.4);
    border-color: rgba(255, 255, 255, 0.4);
  }

  .portrait-frame::after {
    content: '';
    position: absolute;
    inset: var(--space-xs);
    border-radius: calc(var(--radius-lg) - var(--space-xs));
    border: 1px solid rgba(255, 255, 255, 0.2);
    pointer-events: none;
    background-color: transparent;
  }

  .portrait-frame img {
    display: block;
    width: clamp(200px, 26vw, 280px);
    height: auto;
    border-radius: calc(var(--radius-lg) - var(--space-xs));
    position: relative;
    z-index: 1;
  }

  .projects-section {
    padding: var(--space-3xl) 0 var(--space-3xl);
  }

  .projects-heading {
    margin: 0 0 var(--space-3xl) 0;
    font-size: var(--font-size-2xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    position: relative;
    text-align: center;
    letter-spacing: -0.01em;
  }

  .projects-heading::before {
    content: '';
    position: absolute;
    top: 50%;
    left: -80px;
    width: 60px;
    height: 2px;
    background-color: var(--color-accent);
    transform: translateY(-50%);
  }

  .projects-heading::after {
    content: '';
    position: absolute;
    top: 50%;
    right: -80px;
    width: 60px;
    height: 2px;
    background-color: var(--color-accent);
    transform: translateY(-50%);
  }

  .project-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: var(--space-xl);
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    align-items: stretch;
  }

  .project-tile {
    position: relative;
    display: block;
    border-radius: var(--radius-md);
    overflow: hidden;
    background-color: var(--color-surface);
    height: 320px;
    border: 1px solid rgba(17, 34, 21, 0.08);
    box-shadow: 0 12px 28px rgba(17, 34, 21, 0.1);
    background-image: var(--project-image);
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center;
    transition: all var(--duration-md) var(--ease-standard);
    transform: translateY(0) scale(1);
  }

  .project-tile::before {
    content: '';
    position: absolute;
    inset: 0;
    background-color: rgba(17, 34, 21, 0.68);
    opacity: 0.9;
    transition: opacity var(--duration-md) var(--ease-standard);
    z-index: 0;
  }

  .project-tile:hover,
  .project-tile:focus-visible {
    transform: translateY(-12px) scale(1.03);
    box-shadow: 0 32px 64px rgba(17, 34, 21, 0.25), 0 0 0 2px var(--color-accent);
  }

  .project-tile:hover::before,
  .project-tile:focus-visible::before {
    opacity: 0.6;
  }

  .project-tile:hover .project-content {
    transform: translateY(-4px);
  }

  .project-tile:hover .project-index {
    color: var(--color-accent);
    transform: scale(1.1);
  }

  .project-overlay {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    height: 100%;
    padding: var(--space-xl) var(--space-lg) var(--space-2xl) var(--space-lg);
    color: var(--color-surface);
  }

  .project-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
    flex-grow: 1;
    justify-content: flex-end;
    transition: transform 0.3s ease;
  }

  .project-index {
    font-size: var(--font-size-xs);
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.68);
    transition: all 0.3s ease;
    font-weight: var(--font-weight-semibold);
  }

  .project-heading {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .project-heading h3 {
    margin: 0;
    color: white;
    font-size: clamp(1rem, 1.8vw, 1.25rem);
    line-height: 1.3;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
  }

  .project-impact {
    font-size: var(--font-size-xs);
    text-transform: uppercase;
    letter-spacing: 0.14em;
    padding: var(--space-3xs) var(--space-sm);
    border-radius: var(--radius-round);
    background-color: var(--color-accent);
    color: white;
    font-weight: var(--font-weight-semibold);
    box-shadow: 0 4px 8px rgba(79, 124, 99, 0.3);
    width: fit-content;
    align-self: flex-start;
    transition: all 0.3s ease;
    transform: translateY(0);
  }

  .project-tile:hover .project-impact {
    transform: translateY(-2px);
    box-shadow: 0 6px 12px rgba(79, 124, 99, 0.4);
  }

  .project-content p {
    margin: 0;
    color: rgba(255, 255, 255, 0.88);
    max-width: 52ch;
    font-size: var(--font-size-xs);
    line-height: 1.5;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
  }

  .project-tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-xs);
    margin-top: var(--space-xs);
  }

  .project-tags span {
    padding: var(--space-2xs) var(--space-sm);
    border-radius: var(--radius-round);
    background: rgba(93, 138, 111, 0.10);
    border: 1px solid rgba(79, 124, 99, 0.3);
    color: rgba(255, 255, 255, 0.9);
    font-size: 0.65rem;
    letter-spacing: 0.06em;
    font-weight: var(--font-weight-medium);
    backdrop-filter: blur(8px);
    transition: all 0.2s ease;
  }

  .project-tile:hover .project-tags span {
    background: rgba(255, 255, 255, 0.2);
    border-color: rgba(255, 255, 255, 0.35);
    transform: translateY(-1px);
  }

  .interests-grid {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: var(--space-md);
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  }

  .interest-card {
    padding: var(--space-lg);
    border-radius: var(--radius-sm);
    background-color: rgba(255, 255, 255, 0.18);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    box-shadow: 
      0 8px 32px rgba(0, 0, 0, 0.1),
      inset 0 1px 0 rgba(255, 255, 255, 0.2);
    font-size: var(--font-size-sm);
    color: var(--color-text-primary);
    transition: all 0.3s ease;
  }

  .interest-card:hover {
    transform: translateY(-2px);
    box-shadow: 
      0 12px 40px rgba(0, 0, 0, 0.15),
      inset 0 1px 0 rgba(255, 255, 255, 0.3);
    border-color: rgba(255, 255, 255, 0.3);
  }

  .sandrise-grid {
    display: grid;
    grid-template-columns: repeat(12, minmax(0, 1fr));
    gap: var(--space-2xl);
    align-items: start;
  }

  .sandrise-section {
    padding: var(--space-md) 0 var(--space-lg);
    background-color: rgba(79, 124, 99, 0.04);
    position: relative;
  }

  .sandrise-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background-color: rgba(79, 124, 99, 0.2);
  }

  .sandrise-copy {
    grid-column: span 7;
    display: grid;
    gap: var(--space-md);
  }

  .sandrise-card {
    grid-column: span 5;
    display: grid;
    gap: var(--space-md);
  }

  .sandrise-card ul {
    list-style: none;
    margin: 0;
    display: grid;
    gap: var(--space-sm);
  }

  .sandrise-card li {
    display: grid;
    gap: var(--space-4xs);
    color: var(--color-text-secondary);
  }

  .sandrise-card strong {
    color: var(--color-text-primary);
  }

  @media (max-width: 1080px) {
    .intro-grid {
      grid-template-columns: repeat(1, minmax(0, 1fr));
    }

    .intro-copy,
    .intro-portrait {
      grid-column: span 1;
    }

    .intro-portrait {
      justify-content: center;
    }

    .sandrise-grid {
      grid-template-columns: repeat(1, minmax(0, 1fr));
    }

    .sandrise-copy,
    .sandrise-card {
      grid-column: span 1;
    }
  }

  @media (max-width: 768px) {
    .intro-section {
      padding-top: var(--space-sm);
      padding-bottom: var(--space-lg);
    }

    .portrait-frame {
      padding: var(--space-lg);
    }

    .project-overlay {
      padding: var(--space-md);
      gap: var(--space-sm);
    }

    .interest-card {
      padding: var(--space-lg);
    }

  }

  @media (max-width: 520px) {
    .intro-links {
      flex-direction: column;
      align-items: stretch;
    }

    .project-overlay {
      padding: var(--space-lg);
    }

    .project-index {
      font-size: var(--font-size-xs);
    }
  }

  /* Scroll-triggered animation classes */
  .scroll-trigger {
    opacity: 1;
  }

  .scroll-fade-up {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .scroll-fade-up.animate {
    opacity: 1;
    transform: translateY(0);
  }

  .scroll-fade-left {
    opacity: 0;
    transform: translateX(-40px);
    transition: all 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .scroll-fade-left.animate {
    opacity: 1;
    transform: translateX(0);
  }

  .scroll-fade-right {
    opacity: 0;
    transform: translateX(40px);
    transition: all 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .scroll-fade-right.animate {
    opacity: 1;
    transform: translateX(0);
  }

  .scroll-scale-in {
    opacity: 0;
    transform: scale(0.95) translateY(20px);
    transition: all 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    transition-delay: var(--animation-delay, 0s);
  }

  .scroll-scale-in.animate {
    opacity: 1;
    transform: scale(1) translateY(0);
  }

  .scroll-slide-in {
    opacity: 0;
    transform: translateY(25px);
    transition: all 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    transition-delay: var(--animation-delay, 0s);
  }

  .scroll-slide-in.animate {
    opacity: 1;
    transform: translateY(0);
  }

  /* Respect reduced motion preferences */
  @media (prefers-reduced-motion: reduce) {
    .scroll-fade-up,
    .scroll-fade-left,
    .scroll-fade-right,
    .scroll-scale-in,
    .scroll-slide-in {
      transition: none;
      opacity: 1;
      transform: none;
    }
  }

  /* Keyframe Animations */
  @keyframes slideInLeft {
    from {
      opacity: 0;
      transform: translateX(-30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes slideInRight {
    from {
      opacity: 0;
      transform: translateX(30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fadeInLeft {
    from {
      opacity: 0;
      transform: translateX(-30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes fadeInRight {
    from {
      opacity: 0;
      transform: translateX(30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes scaleIn {
    from {
      opacity: 0;
      transform: scale(0.9) translateY(20px);
    }
    to {
      opacity: 1;
      transform: scale(1) translateY(0);
    }
  }

  @keyframes slideInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes shimmer {
    0% {
      background-position: -200% center;
    }
    100% {
      background-position: 200% center;
    }
  }

</style>