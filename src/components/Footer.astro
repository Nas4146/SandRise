---
const year = new Date().getFullYear();
---
<footer class="site-footer" aria-label="Footer">
  <div class="container footer-content">
    <a class="footer-brand" href="/" aria-label="SandRise home">
      <img src="/sandrise-logo.svg" alt="SandRise logo" class="brand-logo" />
      <span class="brand-label">
        <strong>SandRise</strong>
        <span>Product Studio</span>
      </span>
    </a>
    <nav class="footer-links" aria-label="Footer links">
      <a href="/resume.pdf" download target="_blank" rel="noopener">Resume</a>
      <a href="https://www.linkedin.com/in/sandersnick/" target="_blank" rel="noopener">LinkedIn</a>
      <a href="https://github.com/Nas4146" target="_blank" rel="noopener">GitHub</a>
      <a href="mailto:Nick.sanders.a@gmail.com">Email</a>
    </nav>
  </div>
  <div class="footer-bottom">
    <div class="container footer-bottom-inner">
      <p>© {year} SandRise · Built by Nick Sanders</p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid rgba(17, 34, 21, 0.1);
    padding: var(--space-2xl) 0 var(--space-xl);
    background-color: rgba(79, 124, 99, 0.08);
  }

  .footer-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: var(--space-2xl);
    flex-wrap: wrap;
  }

  .footer-brand {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    color: var(--color-text-primary);
    text-decoration: none;
  }

  .footer-brand:hover .brand-logo {
    transform: translateY(-2px);
  }

  .brand-logo {
    width: 32px;
    height: 32px;
    transition: transform var(--duration-sm) var(--ease-standard);
  }

  .brand-label {
    display: flex;
    flex-direction: column;
    font-size: var(--font-size-sm);
    line-height: 1.1;
  }

  .footer-links {
    display: inline-flex;
    flex-wrap: wrap;
    gap: var(--space-lg);
    font-size: var(--font-size-xs);
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .footer-links a {
    color: var(--color-text-secondary);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    padding-bottom: 0.1rem;
    transition: color var(--duration-sm) var(--ease-standard),
      border-color var(--duration-sm) var(--ease-standard);
  }

  .footer-links a:hover,
  .footer-links a:focus-visible {
    color: var(--color-text-primary);
    border-color: rgba(17, 34, 21, 0.2);
  }

  .footer-bottom {
    margin-top: var(--space-2xl);
    border-top: 1px solid rgba(17, 34, 21, 0.08);
    padding-top: var(--space-md);
  }

  .footer-bottom-inner {
    display: flex;
    justify-content: center;
    font-size: var(--font-size-xs);
    color: var(--color-text-tertiary);
  }

  @media (max-width: 600px) {
    .footer-content {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--space-lg);
    }

    .footer-links {
      gap: var(--space-sm);
    }
  }
</style>
