<header class="site-header">
  <div class="container header-content">
    <a class="brand" href="/" aria-label="SandRise home">
      <span class="brand-mark" aria-hidden="true">SR</span>
      <span class="brand-label">
        <strong>SandRise</strong>
      </span>
    </a>
    <nav class="main-nav" aria-label="Main navigation">
      <ul class="nav-list">
        <li><a href="mailto:Nick.sanders.a@gmail.com" class="nav-link contact-cta">Contact</a></li>
      </ul>
    </nav>
  </div>
</header>

<script>
  // Add entrance animation to header
  const header = document.querySelector('.site-header');
  if (header) {
    header.style.transform = 'translateY(-100%)';
    header.style.transition = 'transform 0.6s cubic-bezier(0.34, 1.56, 0.64, 1)';
    
    requestAnimationFrame(() => {
      header.style.transform = 'translateY(0)';
    });
  }
</script>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 100;
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    background-color: color-mix(in srgb, var(--color-surface) 85%, transparent 15%);
    border-bottom: 1px solid rgba(255, 255, 255, 0.2);
    box-shadow: 
      0 1px 0 rgba(255, 255, 255, 0.1) inset,
      0 8px 32px rgba(17, 34, 21, 0.08);
  }

  .header-content {
    height: var(--nav-height);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-md);
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    font-size: var(--font-size-md);
    font-weight: var(--font-weight-medium);
    color: var(--color-text-primary);
    text-decoration: none;
    transition: all var(--duration-sm) var(--ease-standard);
  }

  .brand:hover .brand-mark {
    transform: translateY(-1px);
    box-shadow: 
      0 1px 0 rgba(255, 255, 255, 0.7) inset,
      0 6px 20px rgba(17, 34, 21, 0.15),
      0 3px 12px rgba(93, 138, 111, 0.08);
  }

  .brand-mark {
    width: 38px;
    height: 38px;
    display: grid;
    place-items: center;
    border-radius: 14px;
    background-color: rgba(255, 255, 255, 0.3);
    backdrop-filter: blur(15px);
    -webkit-backdrop-filter: blur(15px);
    border: 1px solid rgba(255, 255, 255, 0.3);
    color: var(--color-text-primary);
    font-size: 0.85rem;
    font-weight: 700;
    box-shadow: 
      0 1px 0 rgba(255, 255, 255, 0.5) inset,
      0 4px 16px rgba(17, 34, 21, 0.1),
      0 2px 8px rgba(93, 138, 111, 0.04);
    transition: all var(--duration-sm) var(--ease-standard);
    position: relative;
  }

  .brand-mark::after {
    display: none;
  }

  .brand-label {
    display: flex;
    flex-direction: column;
    line-height: 1.1;
    font-weight: var(--font-weight-semibold);
    letter-spacing: -0.02em;
  }

  .brand-label strong {
    font-weight: var(--font-weight-bold);
  }

  .main-nav {
    margin-left: auto;
  }

  .nav-list {
    display: flex;
    align-items: center;
    gap: var(--space-lg);
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .nav-link {
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-secondary);
    text-decoration: none;
    padding: var(--space-xs) var(--space-sm);
    border-radius: var(--radius-sm);
    transition: all var(--duration-sm) var(--ease-standard);
    position: relative;
    letter-spacing: 0.01em;
  }

  .nav-link:hover,
  .nav-link:focus-visible {
    color: var(--color-text-primary);
    background-color: rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    transform: translateY(-1px);
    box-shadow: 
      0 1px 0 rgba(255, 255, 255, 0.4) inset,
      0 2px 8px rgba(93, 138, 111, 0.08);
  }

  .nav-link.contact-cta {
    background-color: rgba(255, 255, 255, 0.3);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.4);
    color: var(--color-accent);
    font-weight: var(--font-weight-semibold);
    padding: var(--space-xs) var(--space-md);
    box-shadow: 
      0 1px 0 rgba(255, 255, 255, 0.6) inset,
      0 4px 16px rgba(93, 138, 111, 0.08);
    position: relative;
  }

  .nav-link.contact-cta::after {
    display: none;
  }

  .nav-link.contact-cta:hover,
  .nav-link.contact-cta:focus-visible {
    background-color: rgba(255, 255, 255, 0.38);
    border-color: rgba(255, 255, 255, 0.6);
    transform: translateY(-2px);
    box-shadow: 
      0 1px 0 rgba(255, 255, 255, 0.8) inset,
      0 8px 24px rgba(93, 138, 111, 0.15),
      0 4px 16px rgba(93, 138, 111, 0.08);
  }

  @media (max-width: 768px) {
    .nav-list {
      gap: var(--space-md);
    }
    
    .nav-link {
      font-size: var(--font-size-xs);
      padding: var(--space-2xs) var(--space-xs);
    }
    
    .nav-link.contact-cta {
      padding: var(--space-2xs) var(--space-sm);
    }
  }

  @media (max-width: 520px) {
    .header-content {
      gap: var(--space-sm);
    }
    
    .nav-list {
      gap: var(--space-sm);
    }
  }

</style>
