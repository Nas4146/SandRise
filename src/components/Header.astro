<header class="site-header">
  <div class="container header-content">
    <a class="brand" href="/" aria-label="SandRise home">
      <span class="brand-mark" aria-hidden="true">SR</span>
      <span class="brand-label">
        <strong>SandRise</strong>
      </span>
    </a>
    <nav class="main-nav" aria-label="Main navigation">
      <ul class="nav-list">
        <li><a href="mailto:Nick.sanders.a@gmail.com" class="nav-link contact-cta">Contact</a></li>
      </ul>
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 100;
    backdrop-filter: blur(14px);
    background:
      color-mix(in srgb, var(--color-surface) 90%, transparent 10%);
    border-bottom: 1px solid rgba(17, 34, 21, 0.08);
  }

  .header-content {
    height: var(--nav-height);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-md);
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    font-size: var(--font-size-md);
    font-weight: var(--font-weight-medium);
    color: var(--color-text-primary);
  }

  .brand-mark {
    width: 38px;
    height: 38px;
    display: grid;
    place-items: center;
    border-radius: 14px;
    background: rgba(17, 34, 21, 0.08);
    border: 1px solid rgba(17, 34, 21, 0.12);
    color: var(--color-text-primary);
    font-size: 0.85rem;
    font-weight: 600;
  }

  .brand-label {
    display: flex;
    flex-direction: column;
    line-height: 1.1;
  }

  .main-nav {
    margin-left: auto;
  }

  .nav-list {
    display: flex;
    align-items: center;
    gap: var(--space-lg);
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .nav-link {
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-medium);
    color: var(--color-text-secondary);
    text-decoration: none;
    padding: var(--space-xs) var(--space-sm);
    border-radius: var(--radius-sm);
    transition: all var(--duration-sm) var(--ease-standard);
    position: relative;
  }

  .nav-link:hover,
  .nav-link:focus-visible {
    color: var(--color-text-primary);
    background: rgba(12, 143, 98, 0.06);
  }

  .nav-link.contact-cta {
    background: linear-gradient(135deg, rgba(12, 143, 98, 0.1), rgba(47, 190, 132, 0.05));
    border: 1px solid rgba(12, 143, 98, 0.15);
    color: var(--color-accent);
    font-weight: var(--font-weight-semibold);
    padding: var(--space-xs) var(--space-md);
  }

  .nav-link.contact-cta:hover,
  .nav-link.contact-cta:focus-visible {
    background: linear-gradient(135deg, rgba(12, 143, 98, 0.15), rgba(47, 190, 132, 0.08));
    border-color: rgba(12, 143, 98, 0.25);
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(12, 143, 98, 0.15);
  }

  @media (max-width: 768px) {
    .nav-list {
      gap: var(--space-md);
    }
    
    .nav-link {
      font-size: var(--font-size-xs);
      padding: var(--space-2xs) var(--space-xs);
    }
    
    .nav-link.contact-cta {
      padding: var(--space-2xs) var(--space-sm);
    }
  }

  @media (max-width: 520px) {
    .header-content {
      gap: var(--space-sm);
    }
    
    .nav-list {
      gap: var(--space-sm);
    }
  }

</style>
