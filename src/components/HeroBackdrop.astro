<div class="hero-backdrop" aria-hidden="true">
  <div class="gradient"></div>
  <div class="grid"></div>
</div>

<style>
  .hero-backdrop {
    position: absolute;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .gradient {
    position: absolute;
    inset: 0;
    background-color: rgba(111, 157, 129, 0.12);
    filter: blur(0px);
  }

  .grid {
    position: absolute;
    inset: var(--space-2xl) var(--space-xl);
    border-radius: var(--radius-lg);
    border: 1px solid rgba(17, 34, 21, 0.1);
    background-color: transparent;
    opacity: 0.6;
  }

  @media (prefers-reduced-motion: reduce) {
    .gradient {
      opacity: 0.4;
    }
  }
</style>
