<div class="hero-backdrop" aria-hidden="true">
  <div class="gradient"></div>
  <div class="grid"></div>
</div>

<style>
  .hero-backdrop {
    position: absolute;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(165deg, rgba(11, 143, 98, 0.16), rgba(47, 190, 132, 0));
    filter: blur(0px);
  }

  .grid {
    position: absolute;
    inset: var(--space-2xl) var(--space-xl);
    border-radius: var(--radius-lg);
    border: 1px solid rgba(17, 34, 21, 0.1);
    background-image: linear-gradient(
        to right,
        rgba(17, 34, 21, 0.08) 1px,
        transparent 1px
      ),
      linear-gradient(
        to bottom,
        rgba(17, 34, 21, 0.08) 1px,
        transparent 1px
      );
    background-size: 36px 36px;
    opacity: 0.6;
  }

  @media (prefers-reduced-motion: reduce) {
    .gradient {
      opacity: 0.4;
    }
  }
</style>
